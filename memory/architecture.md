# System Architecture

> Generated by **techlead_agent** at 2026-04-05T04:27:40Z

## Tech Stack

- **Frontend**: Next.js 14 + TypeScript 5 + TailwindCSS
- **Backend**: FastAPI + Python 3.11+
- **Database**: PostgreSQL 16
- **Cache**: Redis
- **Auth**: JWT (access 15min, refresh 7d)

## Components

| Component | Description |
|-----------|-------------|
| Auth Service | JWT authentication and authorization |
| Policy Service | Policy CRUD and lifecycle management |
| Claims Service | Claims submission, review, and settlement |
| User Service | User profile and role management |
| Notification Service | Email, SMS, and push notifications |
| Document Service | Document upload and storage |

## API Contracts

| Method | Path | Summary | Response |
|--------|------|---------|----------|
| GET | `/api/v1/auths` | List all auths | 200 — array of resources |
| POST | `/api/v1/auths` | Create a new auth | 201 — created resource |
| GET | `/api/v1/auths/{id}` | Get auth by ID | 200 — single resource |
| PUT | `/api/v1/auths/{id}` | Update auth | 200 — updated resource |
| GET | `/api/v1/policys` | List all policys | 200 — array of resources |
| POST | `/api/v1/policys` | Create a new policy | 201 — created resource |
| GET | `/api/v1/policys/{id}` | Get policy by ID | 200 — single resource |
| PUT | `/api/v1/policys/{id}` | Update policy | 200 — updated resource |
| GET | `/api/v1/claimss` | List all claimss | 200 — array of resources |
| POST | `/api/v1/claimss` | Create a new claims | 201 — created resource |
| GET | `/api/v1/claimss/{id}` | Get claims by ID | 200 — single resource |
| PUT | `/api/v1/claimss/{id}` | Update claims | 200 — updated resource |
| GET | `/api/v1/users` | List all users | 200 — array of resources |
| POST | `/api/v1/users` | Create a new user | 201 — created resource |
| GET | `/api/v1/users/{id}` | Get user by ID | 200 — single resource |
| PUT | `/api/v1/users/{id}` | Update user | 200 — updated resource |
| GET | `/api/v1/notifications` | List all notifications | 200 — array of resources |
| POST | `/api/v1/notifications` | Create a new notification | 201 — created resource |
| GET | `/api/v1/notifications/{id}` | Get notification by ID | 200 — single resource |
| PUT | `/api/v1/notifications/{id}` | Update notification | 200 — updated resource |
| GET | `/api/v1/documents` | List all documents | 200 — array of resources |
| POST | `/api/v1/documents` | Create a new document | 201 — created resource |
| GET | `/api/v1/documents/{id}` | Get document by ID | 200 — single resource |
| PUT | `/api/v1/documents/{id}` | Update document | 200 — updated resource |

## Database Schema

### `auths`

| Column | Type | Constraints |
|--------|------|-------------|
| id | UUID | PRIMARY KEY DEFAULT gen_random_uuid() |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| status | VARCHAR(50) | NOT NULL DEFAULT 'active' |

**Indexes**: idx_auths_status, idx_auths_created_at

### `policys`

| Column | Type | Constraints |
|--------|------|-------------|
| id | UUID | PRIMARY KEY DEFAULT gen_random_uuid() |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| status | VARCHAR(50) | NOT NULL DEFAULT 'active' |

**Indexes**: idx_policys_status, idx_policys_created_at

### `claimss`

| Column | Type | Constraints |
|--------|------|-------------|
| id | UUID | PRIMARY KEY DEFAULT gen_random_uuid() |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| status | VARCHAR(50) | NOT NULL DEFAULT 'active' |

**Indexes**: idx_claimss_status, idx_claimss_created_at

### `users`

| Column | Type | Constraints |
|--------|------|-------------|
| id | UUID | PRIMARY KEY DEFAULT gen_random_uuid() |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| status | VARCHAR(50) | NOT NULL DEFAULT 'active' |

**Indexes**: idx_users_status, idx_users_created_at

### `notifications`

| Column | Type | Constraints |
|--------|------|-------------|
| id | UUID | PRIMARY KEY DEFAULT gen_random_uuid() |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| status | VARCHAR(50) | NOT NULL DEFAULT 'active' |

**Indexes**: idx_notifications_status, idx_notifications_created_at

### `documents`

| Column | Type | Constraints |
|--------|------|-------------|
| id | UUID | PRIMARY KEY DEFAULT gen_random_uuid() |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT now() |
| status | VARCHAR(50) | NOT NULL DEFAULT 'active' |

**Indexes**: idx_documents_status, idx_documents_created_at
