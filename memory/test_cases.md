# Test Cases

> Generated by **qa_agent** at 2026-04-05T11:30:12Z

| ID | Type | Title | Priority |
|----|------|-------|----------|
| TC-001 | API | GET /api/v1/auths — happy path | High |
| TC-002 | API | GET /api/v1/auths — invalid input | Medium |
| TC-003 | API | POST /api/v1/auths — happy path | High |
| TC-004 | API | POST /api/v1/auths — invalid input | Medium |
| TC-005 | API | GET /api/v1/auths/{id} — happy path | High |
| TC-006 | API | GET /api/v1/auths/{id} — invalid input | Medium |
| TC-007 | API | PUT /api/v1/auths/{id} — happy path | High |
| TC-008 | API | PUT /api/v1/auths/{id} — invalid input | Medium |
| TC-009 | API | GET /api/v1/policys — happy path | High |
| TC-010 | API | GET /api/v1/policys — invalid input | Medium |
| TC-011 | API | POST /api/v1/policys — happy path | High |
| TC-012 | API | POST /api/v1/policys — invalid input | Medium |
| TC-013 | API | GET /api/v1/policys/{id} — happy path | High |
| TC-014 | API | GET /api/v1/policys/{id} — invalid input | Medium |
| TC-015 | API | PUT /api/v1/policys/{id} — happy path | High |
| TC-016 | API | PUT /api/v1/policys/{id} — invalid input | Medium |
| TC-017 | API | GET /api/v1/claimss — happy path | High |
| TC-018 | API | GET /api/v1/claimss — invalid input | Medium |
| TC-019 | API | POST /api/v1/claimss — happy path | High |
| TC-020 | API | POST /api/v1/claimss — invalid input | Medium |
| TC-021 | API | GET /api/v1/claimss/{id} — happy path | High |
| TC-022 | API | GET /api/v1/claimss/{id} — invalid input | Medium |
| TC-023 | API | PUT /api/v1/claimss/{id} — happy path | High |
| TC-024 | API | PUT /api/v1/claimss/{id} — invalid input | Medium |
| TC-025 | API | GET /api/v1/users — happy path | High |
| TC-026 | API | GET /api/v1/users — invalid input | Medium |
| TC-027 | API | POST /api/v1/users — happy path | High |
| TC-028 | API | POST /api/v1/users — invalid input | Medium |
| TC-029 | API | GET /api/v1/users/{id} — happy path | High |
| TC-030 | API | GET /api/v1/users/{id} — invalid input | Medium |
| TC-031 | API | PUT /api/v1/users/{id} — happy path | High |
| TC-032 | API | PUT /api/v1/users/{id} — invalid input | Medium |
| TC-033 | API | GET /api/v1/notifications — happy path | High |
| TC-034 | API | GET /api/v1/notifications — invalid input | Medium |
| TC-035 | API | POST /api/v1/notifications — happy path | High |
| TC-036 | API | POST /api/v1/notifications — invalid input | Medium |
| TC-037 | API | GET /api/v1/notifications/{id} — happy path | High |
| TC-038 | API | GET /api/v1/notifications/{id} — invalid input | Medium |
| TC-039 | API | PUT /api/v1/notifications/{id} — happy path | High |
| TC-040 | API | PUT /api/v1/notifications/{id} — invalid input | Medium |
| TC-041 | API | GET /api/v1/quotings — happy path | High |
| TC-042 | API | GET /api/v1/quotings — invalid input | Medium |
| TC-043 | API | POST /api/v1/quotings — happy path | High |
| TC-044 | API | POST /api/v1/quotings — invalid input | Medium |
| TC-045 | API | GET /api/v1/quotings/{id} — happy path | High |
| TC-046 | API | GET /api/v1/quotings/{id} — invalid input | Medium |
| TC-047 | API | PUT /api/v1/quotings/{id} — happy path | High |
| TC-048 | API | PUT /api/v1/quotings/{id} — invalid input | Medium |
| TC-049 | API | GET /api/v1/documents — happy path | High |
| TC-050 | API | GET /api/v1/documents — invalid input | Medium |
| TC-051 | API | POST /api/v1/documents — happy path | High |
| TC-052 | API | POST /api/v1/documents — invalid input | Medium |
| TC-053 | API | GET /api/v1/documents/{id} — happy path | High |
| TC-054 | API | GET /api/v1/documents/{id} — invalid input | Medium |
| TC-055 | API | PUT /api/v1/documents/{id} — happy path | High |
| TC-056 | API | PUT /api/v1/documents/{id} — invalid input | Medium |
| TC-057 | Acceptance | Verify: As an insurance platform user, I want build a policy management module for auto  | High |
| TC-058 | Security | Auth — expired JWT rejected | Critical |
| TC-059 | Security | Auth — missing token rejected | Critical |

## Detailed Steps

### TC-001: GET /api/v1/auths — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/auths with valid data

**Expected**: Returns appropriate success status code

### TC-002: GET /api/v1/auths — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/auths with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-003: POST /api/v1/auths — happy path

**Type**: API  
**Priority**: High

**Steps**: Send POST to /api/v1/auths with valid data

**Expected**: Returns appropriate success status code

### TC-004: POST /api/v1/auths — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send POST to /api/v1/auths with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-005: GET /api/v1/auths/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/auths/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-006: GET /api/v1/auths/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/auths/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-007: PUT /api/v1/auths/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send PUT to /api/v1/auths/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-008: PUT /api/v1/auths/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send PUT to /api/v1/auths/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-009: GET /api/v1/policys — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/policys with valid data

**Expected**: Returns appropriate success status code

### TC-010: GET /api/v1/policys — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/policys with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-011: POST /api/v1/policys — happy path

**Type**: API  
**Priority**: High

**Steps**: Send POST to /api/v1/policys with valid data

**Expected**: Returns appropriate success status code

### TC-012: POST /api/v1/policys — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send POST to /api/v1/policys with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-013: GET /api/v1/policys/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/policys/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-014: GET /api/v1/policys/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/policys/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-015: PUT /api/v1/policys/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send PUT to /api/v1/policys/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-016: PUT /api/v1/policys/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send PUT to /api/v1/policys/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-017: GET /api/v1/claimss — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/claimss with valid data

**Expected**: Returns appropriate success status code

### TC-018: GET /api/v1/claimss — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/claimss with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-019: POST /api/v1/claimss — happy path

**Type**: API  
**Priority**: High

**Steps**: Send POST to /api/v1/claimss with valid data

**Expected**: Returns appropriate success status code

### TC-020: POST /api/v1/claimss — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send POST to /api/v1/claimss with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-021: GET /api/v1/claimss/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/claimss/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-022: GET /api/v1/claimss/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/claimss/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-023: PUT /api/v1/claimss/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send PUT to /api/v1/claimss/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-024: PUT /api/v1/claimss/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send PUT to /api/v1/claimss/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-025: GET /api/v1/users — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/users with valid data

**Expected**: Returns appropriate success status code

### TC-026: GET /api/v1/users — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/users with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-027: POST /api/v1/users — happy path

**Type**: API  
**Priority**: High

**Steps**: Send POST to /api/v1/users with valid data

**Expected**: Returns appropriate success status code

### TC-028: POST /api/v1/users — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send POST to /api/v1/users with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-029: GET /api/v1/users/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/users/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-030: GET /api/v1/users/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/users/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-031: PUT /api/v1/users/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send PUT to /api/v1/users/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-032: PUT /api/v1/users/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send PUT to /api/v1/users/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-033: GET /api/v1/notifications — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/notifications with valid data

**Expected**: Returns appropriate success status code

### TC-034: GET /api/v1/notifications — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/notifications with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-035: POST /api/v1/notifications — happy path

**Type**: API  
**Priority**: High

**Steps**: Send POST to /api/v1/notifications with valid data

**Expected**: Returns appropriate success status code

### TC-036: POST /api/v1/notifications — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send POST to /api/v1/notifications with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-037: GET /api/v1/notifications/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/notifications/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-038: GET /api/v1/notifications/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/notifications/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-039: PUT /api/v1/notifications/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send PUT to /api/v1/notifications/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-040: PUT /api/v1/notifications/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send PUT to /api/v1/notifications/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-041: GET /api/v1/quotings — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/quotings with valid data

**Expected**: Returns appropriate success status code

### TC-042: GET /api/v1/quotings — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/quotings with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-043: POST /api/v1/quotings — happy path

**Type**: API  
**Priority**: High

**Steps**: Send POST to /api/v1/quotings with valid data

**Expected**: Returns appropriate success status code

### TC-044: POST /api/v1/quotings — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send POST to /api/v1/quotings with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-045: GET /api/v1/quotings/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/quotings/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-046: GET /api/v1/quotings/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/quotings/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-047: PUT /api/v1/quotings/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send PUT to /api/v1/quotings/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-048: PUT /api/v1/quotings/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send PUT to /api/v1/quotings/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-049: GET /api/v1/documents — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/documents with valid data

**Expected**: Returns appropriate success status code

### TC-050: GET /api/v1/documents — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/documents with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-051: POST /api/v1/documents — happy path

**Type**: API  
**Priority**: High

**Steps**: Send POST to /api/v1/documents with valid data

**Expected**: Returns appropriate success status code

### TC-052: POST /api/v1/documents — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send POST to /api/v1/documents with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-053: GET /api/v1/documents/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send GET to /api/v1/documents/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-054: GET /api/v1/documents/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send GET to /api/v1/documents/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-055: PUT /api/v1/documents/{id} — happy path

**Type**: API  
**Priority**: High

**Steps**: Send PUT to /api/v1/documents/{id} with valid data

**Expected**: Returns appropriate success status code

### TC-056: PUT /api/v1/documents/{id} — invalid input

**Type**: API  
**Priority**: Medium

**Steps**: Send PUT to /api/v1/documents/{id} with invalid/missing data

**Expected**: Returns 4xx with RFC 7807 error body

### TC-057: Verify: As an insurance platform user, I want build a policy management module for auto 

**Type**: Acceptance  
**Priority**: High

**Steps**: Execute the user journey described in: As an insurance platform user, I want build a policy management module for auto insurance where:- users can generate a quote based on vehicle and driver details, premium is calculated using predefined risk rules, users can purchase a policy and download policy documents, so that the platform meets business needs.

**Expected**: User achieves the stated goal

### TC-058: Auth — expired JWT rejected

**Type**: Security  
**Priority**: Critical

**Steps**: Send request with expired JWT token

**Expected**: Returns 401 Unauthorized

### TC-059: Auth — missing token rejected

**Type**: Security  
**Priority**: Critical

**Steps**: Send request without Authorization header

**Expected**: Returns 401 Unauthorized
